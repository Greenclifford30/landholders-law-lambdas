# Lambda-specific requirements for post-admin-image-upload-url-lambda
# Common dependencies (boto3, pydantic, python-jose, requests) provided by shared layer

# This function only needs the shared layer dependencies
# No additional lambda-specific dependencies required